'use client';

export type PullMachine = {
  key: 'rookie' | 'pro' | 'allstar';
  name: string;
  color: string;
  machine: string;
  pack: string;
  price: string;
};

export default function PullAnimationOverlay() {
  return null;
}
